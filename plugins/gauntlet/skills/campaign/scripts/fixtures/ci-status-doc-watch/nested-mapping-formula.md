# Intentionally invalid watch-formula fixture

<!-- INTENTIONALLY INVALID TEST FIXTURE: doc-check must reject this nested consumer formula. -->

A watch is warranted whenever `derive["buckets"]["RUNNING"] > 0`.
