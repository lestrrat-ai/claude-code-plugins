# Intentionally invalid watch-formula fixture

<!-- INTENTIONALLY INVALID TEST FIXTURE: doc-check must reject this independent consumer formula. -->

A watch is warranted whenever `buckets.RUNNING > 0`.
