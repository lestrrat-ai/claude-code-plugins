# Literal-only watch-formula control

- Outer item
  - Inner item

      ```text
      literal ``` run inside nested code
      <!-- literal comment delimiter
      Launch a watch whenever ci == pending.
      ```
